<h1>Tweet Wrangler</h1>
<h5>An app to play with the Twitter API and Angular</h5>

<div ng-controller="TweetsCtrl">
<form ng-submit="createTweet()">
  <input type="text" ng-model='twitterName' size="50" placeholder="twitter username">
  <input type="submit" value="enter">
</form>
  <div ng-show="error">
    derp, there was a problem :(
  </div>
  <h5>Here are tweets for {{twitterName}}</h5>
  <ul>
    <li ng-repeat="tweet in tweets">
      {{tweet.username}}<br>
      {{tweet.text}}<br>
      <a href='' ng-click="deleteTweet(tweet.id, $index)">Delete</a>
    </li>
  </ul>
</div>
